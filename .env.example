# Server-side Google Gemini API Key (kept secret)
# Get your API key from: https://aistudio.google.com/app/apikey
# Set this in your local environment (e.g., .env.local) as shown below and restart the dev server
GEMINI_API_KEY=your_api_key_here

# A secret used to sign JWT session tokens. Set this before using auth in production.
JWT_SECRET=change_me_to_a_strong_random_value
